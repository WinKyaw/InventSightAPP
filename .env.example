# InventSight Backend Configuration
# =====================================
# Copy this file to .env and update with your values
# After changes, restart the Expo app completely!

# Backend API URL
# =====================================
# CRITICAL: Replace with your actual backend IP address
# 
# IMPORTANT FOR BACKEND: 
# The backend AuthController is DISABLED by default!
# You MUST enable it in backend's application.yml:
#
#   inventsight:
#     security:
#       local-login:
#         enabled: true  # ← REQUIRED!
#
# Without this, the /api/auth/login endpoint doesn't exist!
# See BACKEND_SETUP.md for complete setup instructions.
#
# Find your IP address:
# - Mac:     ipconfig getifaddr en0
# - Windows: ipconfig (look for IPv4 Address)
# - Linux:   hostname -I | awk '{print $1}'
#
# Platform-specific examples:
# - Android Emulator: http://10.0.2.2:8080
# - iOS Simulator:    http://localhost:8080
# - Physical Device:  http://192.168.1.100:8080 (use YOUR actual IP)
# - Production:       https://your-api-domain.com
#
API_BASE_URL=http://localhost:8080

# API Timeout (milliseconds)
# =====================================
# Increased to 30 seconds (was 10 seconds) for better reliability
# If backend is slow to start or process requests, increase this value
API_TIMEOUT=30000

# User Configuration
USER_LOGIN=WinKyaw

# Development Settings
# =====================================
# Enable detailed logging for debugging
ENABLE_LOGGING=true

# Development/Demo Settings
# Set this to false to disable demo mode and use real API
DEMO_MODE=false